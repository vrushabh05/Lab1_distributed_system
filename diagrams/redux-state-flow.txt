# Redux State Management Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                      REDUX ARCHITECTURE                              │
└──────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                             │
│                    (React Components)                               │
│                                                                     │
│  ┌───────────────┐  ┌───────────────┐  ┌────────────────┐         │
│  │  Login Page   │  │  Search Page  │  │  Bookings Page │         │
│  │               │  │               │  │                │         │
│  │  • Email      │  │  • Location   │  │  • Booking     │         │
│  │  • Password   │  │  • Dates      │  │    List        │         │
│  │  • Submit     │  │  • Guests     │  │  • Status      │         │
│  └───────┬───────┘  └───────┬───────┘  └────────┬───────┘         │
│          │                  │                    │                 │
│          │ 1. Dispatch      │ 1. Dispatch        │ 1. Dispatch     │
│          │    Action        │    Action          │    Action       │
│          ↓                  ↓                    ↓                 │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              useDispatch() Hook                          │     │
│  │                                                           │     │
│  │  dispatch(login(credentials))                            │     │
│  │  dispatch(searchProperties(filters))                     │     │
│  │  dispatch(fetchBookings())                               │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         REDUX STORE                                 │
│                   (Centralized State)                               │
│                                                                     │
│  store/index.js                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ const store = configureStore({                             │   │
│  │   reducer: {                                                │   │
│  │     auth: authReducer,           ← User auth state         │   │
│  │     properties: propertiesReducer, ← Property search       │   │
│  │     bookings: bookingsReducer,     ← Booking data          │   │
│  │     favorites: favoritesReducer,   ← Favorite properties   │   │
│  │     dashboard: dashboardReducer    ← Owner stats           │   │
│  │   }                                                         │   │
│  │ });                                                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                             │                                       │
│                             │ 2. Action routed to                   │
│                             │    appropriate slice                  │
│                             ↓                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                      SLICES (Reducers)                      │   │
│  │                                                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  authSlice.js                                          │ │   │
│  │  │                                                         │ │   │
│  │  │  State: {                                              │ │   │
│  │  │    user: null,          ← User info                   │ │   │
│  │  │    token: null,         ← JWT token                   │ │   │
│  │  │    isAuthenticated: false,                            │ │   │
│  │  │    loading: false,                                    │ │   │
│  │  │    error: null                                        │ │   │
│  │  │  }                                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  Actions:                                              │ │   │
│  │  │  • login(credentials)    ← Async thunk                │ │   │
│  │  │  • signup(userData)      ← Async thunk                │ │   │
│  │  │  • logout()              ← Sync action                │ │   │
│  │  │  • checkAuth()           ← Async thunk                │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  │                                                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  propertiesSlice.js                                    │ │   │
│  │  │                                                         │ │   │
│  │  │  State: {                                              │ │   │
│  │  │    properties: [],      ← Search results              │ │   │
│  │  │    selectedProperty: null, ← Property details         │ │   │
│  │  │    loading: false,                                    │ │   │
│  │  │    error: null                                        │ │   │
│  │  │  }                                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  Actions:                                              │ │   │
│  │  │  • searchProperties(filters)                          │ │   │
│  │  │  • getPropertyDetails(id)                             │ │   │
│  │  │  • clearSearch()                                      │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  │                                                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  bookingsSlice.js                                      │ │   │
│  │  │                                                         │ │   │
│  │  │  State: {                                              │ │   │
│  │  │    bookings: [],        ← User's bookings             │ │   │
│  │  │    loading: false,                                    │ │   │
│  │  │    error: null                                        │ │   │
│  │  │  }                                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  Actions:                                              │ │   │
│  │  │  • fetchBookings()      ← Get all bookings            │ │   │
│  │  │  • createBooking(data)  ← Create new booking          │ │   │
│  │  │  • cancelBooking(id)    ← Cancel booking              │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  │                                                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  favoritesSlice.js                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  State: {                                              │ │   │
│  │  │    favorites: [],       ← Favorite properties         │ │   │
│  │  │    loading: false,                                    │ │   │
│  │  │    error: null                                        │ │   │
│  │  │  }                                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  Actions:                                              │ │   │
│  │  │  • fetchFavorites()     ← Get all favorites           │ │   │
│  │  │  • addFavorite(id)      ← Add to favorites            │ │   │
│  │  │  • removeFavorite(id)   ← Remove from favorites       │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  │                                                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │  dashboardSlice.js                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  State: {                                              │ │   │
│  │  │    stats: null,         ← Owner dashboard stats       │ │   │
│  │  │    recentBookings: [],  ← Recent booking requests     │ │   │
│  │  │    loading: false,                                    │ │   │
│  │  │    error: null                                        │ │   │
│  │  │  }                                                     │ │   │
│  │  │                                                         │ │   │
│  │  │  Actions:                                              │ │   │
│  │  │  • fetchDashboardStats()                              │ │   │
│  │  │  • fetchRecentBookings()                              │ │   │
│  │  └────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                             │                                       │
│                             │ 3. Reducer updates state              │
│                             ↓                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                  STATE UPDATED                              │   │
│  │                                                              │   │
│  │  Before: { user: null, token: null }                        │   │
│  │  After:  { user: {...}, token: "eyJhbG..." }                │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              │ 4. Notify subscribed components
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                             │
│                    (React Components Re-render)                     │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │           useSelector() Hook                             │      │
│  │                                                           │      │
│  │  const { user, token } = useSelector(state => state.auth)│      │
│  │  const { properties } = useSelector(state =>             │      │
│  │                           state.properties)              │      │
│  │  const { bookings } = useSelector(state =>               │      │
│  │                           state.bookings)                │      │
│  └──────────────────────────────────────────────────────────┘      │
│                             │                                       │
│                             │ 5. Component re-renders with         │
│                             │    new state                          │
│                             ↓                                       │
│  ┌───────────────┐  ┌───────────────┐  ┌────────────────┐         │
│  │  Login Page   │  │  Search Page  │  │  Bookings Page │         │
│  │               │  │               │  │                │         │
│  │  ✅ Logged in │  │  📋 Results   │  │  📅 Bookings   │         │
│  │  → Redirect   │  │  displayed    │  │  Status: ACCEP │         │
│  └───────────────┘  └───────────────┘  └────────────────┘         │
└─────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────┐
│                  ASYNC THUNK FLOW (Example: Login)                   │
└──────────────────────────────────────────────────────────────────────┘

Step 1: User clicks "Login"
   ↓
Step 2: Component dispatches async action
   dispatch(login({ email, password }))
   ↓
Step 3: Redux Toolkit creates THREE actions automatically:
   • login/pending    ← While API call in progress
   • login/fulfilled  ← If API call succeeds
   • login/rejected   ← If API call fails
   ↓
Step 4: Async thunk executes (in authSlice.js)

   export const login = createAsyncThunk(
     'auth/login',
     async (credentials, { rejectWithValue }) => {
       try {
         // 📡 API Call
         const response = await axios.post(
           'http://localhost:3001/api/auth/login',
           credentials
         );
         
         // Store token in localStorage
         localStorage.setItem('token', response.data.token);
         
         return response.data; // ← Becomes action.payload
       } catch (error) {
         return rejectWithValue(error.response.data);
       }
     }
   );

   ↓
Step 5: Reducer handles the action (in authSlice.js)

   const authSlice = createSlice({
     name: 'auth',
     initialState: { user: null, token: null, loading: false, error: null },
     extraReducers: (builder) => {
       builder
         // When API call starts
         .addCase(login.pending, (state) => {
           state.loading = true;
           state.error = null;
         })
         // When API call succeeds
         .addCase(login.fulfilled, (state, action) => {
           state.loading = false;
           state.user = action.payload.user;
           state.token = action.payload.token;
           state.isAuthenticated = true;
         })
         // When API call fails
         .addCase(login.rejected, (state, action) => {
           state.loading = false;
           state.error = action.payload;
         });
     }
   });

   ↓
Step 6: Component re-renders with new state
   • loading: false
   • user: { id, email, role, ... }
   • token: "eyJhbGciOi..."
   • isAuthenticated: true


┌──────────────────────────────────────────────────────────────────────┐
│                  STATE FLOW VISUALIZATION                            │
└──────────────────────────────────────────────────────────────────────┘

SCENARIO 1: User Login
─────────────────────────────────────────────────────────────────────────

 Initial State                    After login/pending
 ┌───────────────┐               ┌───────────────┐
 │ auth: {       │               │ auth: {       │
 │   user: null  │   ────────>   │   user: null  │
 │   token: null │               │   token: null │
 │   loading: ❌ │               │   loading: ✅ │
 │   error: null │               │   error: null │
 │ }             │               │ }             │
 └───────────────┘               └───────────────┘
                                         │
                                         │ API Call
                                         ↓
                                 After login/fulfilled
                                 ┌───────────────┐
                                 │ auth: {       │
                                 │   user: {...} │
                                 │   token: "ey" │
                                 │   loading: ❌ │
                                 │   error: null │
                                 │ }             │
                                 └───────────────┘


SCENARIO 2: Property Search
─────────────────────────────────────────────────────────────────────────

 Initial State                    After search
 ┌───────────────┐               ┌───────────────┐
 │ properties: { │               │ properties: { │
 │   properties: │   ────────>   │   properties: │
 │     []        │               │     [         │
 │   loading: ❌ │               │      {id:1},  │
 │ }             │               │      {id:2}   │
 └───────────────┘               │     ]         │
                                 │   loading: ❌ │
                                 │ }             │
                                 └───────────────┘


SCENARIO 3: Booking Status Update (via Kafka + Redux)
─────────────────────────────────────────────────────────────────────────

 Step 1: Traveler creates booking
 ┌──────────────────┐
 │ bookings: {      │
 │   bookings: [    │
 │     {            │
 │       id: "507", │
 │       status:    │
 │       "PENDING"  │  ← Just created
 │     }            │
 │   ]              │
 │ }                │
 └──────────────────┘
         │
         │ (Kafka message flows: PENDING → ACCEPTED)
         ↓
 Step 2: Traveler refreshes page, Redux fetches updated bookings
 ┌──────────────────┐
 │ bookings: {      │
 │   bookings: [    │
 │     {            │
 │       id: "507", │
 │       status:    │
 │       "ACCEPTED" │  ← Updated! 🎉
 │     }            │
 │   ]              │
 │ }                │
 └──────────────────┘


┌──────────────────────────────────────────────────────────────────────┐
│                      KEY BENEFITS OF REDUX                           │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ✅ Predictable State Management                                    │
│     • Single source of truth                                        │
│     • State changes are explicit and traceable                      │
│                                                                      │
│  ✅ Easy Debugging                                                   │
│     • Redux DevTools show every action and state change             │
│     • Time-travel debugging (replay actions)                        │
│                                                                      │
│  ✅ Component Decoupling                                             │
│     • Components don't need to pass props deeply                    │
│     • Any component can access any part of state                    │
│                                                                      │
│  ✅ Async Handling                                                   │
│     • createAsyncThunk simplifies API calls                         │
│     • Automatic loading/error states                                │
│                                                                      │
│  ✅ Persistence                                                      │
│     • Easy to persist state to localStorage                         │
│     • Restore state on page reload                                  │
└──────────────────────────────────────────────────────────────────────┘
```

## Redux Store Structure

```javascript
{
  auth: {
    user: { id, email, name, role },
    token: "eyJhbGciOiJIUzI1NiIs...",
    isAuthenticated: true,
    loading: false,
    error: null
  },
  properties: {
    properties: [
      { id: 1, name: "Cozy Apartment", price: 150, ... },
      { id: 2, name: "Beach House", price: 300, ... }
    ],
    selectedProperty: { id: 1, name: "Cozy Apartment", ... },
    loading: false,
    error: null
  },
  bookings: {
    bookings: [
      { id: "507f...", propertyId: 1, status: "ACCEPTED", ... },
      { id: "508f...", propertyId: 2, status: "PENDING", ... }
    ],
    loading: false,
    error: null
  },
  favorites: {
    favorites: [1, 3, 5],  // Property IDs
    loading: false,
    error: null
  },
  dashboard: {
    stats: {
      totalProperties: 5,
      totalBookings: 12,
      pendingBookings: 3,
      acceptedBookings: 8
    },
    recentBookings: [ ... ],
    loading: false,
    error: null
  }
}
```

## Viewing Redux State

1. Install Redux DevTools browser extension
2. Open DevTools (F12) → Redux tab
3. See all actions dispatched
4. Inspect current state
5. Time-travel through state changes
