# Kafka Message Flow - Detailed View

```
┌──────────────────────────────────────────────────────────────────────┐
│                    BOOKING LIFECYCLE WITH KAFKA                      │
└──────────────────────────────────────────────────────────────────────┘

PHASE 1: BOOKING CREATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────┐
│  Traveler   │  1. User fills booking form (dates, guests)
│   (User)    │     and clicks "Book Now"
└──────┬──────┘
       │
       │ HTTP POST /api/bookings
       │ Body: { propertyId, ownerId, startDate, 
       │        endDate, totalPrice }
       ↓
┌────────────────────────────────────────────────────────────┐
│             TRAVELER SERVICE (Producer)                    │
│             http://localhost:3001                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Step 2: Receive booking request                          │
│  ┌──────────────────────────────────────────────────┐     │
│  │ POST /api/bookings Handler                       │     │
│  │                                                   │     │
│  │ • Validate JWT token                             │     │
│  │ • Validate dates (not in past, end > start)      │     │
│  │ • Extract traveler ID from JWT                   │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  Step 3: Save to database                                 │
│  ┌──────────────────────────────────────────────────┐     │
│  │ MongoDB Write                                     │     │
│  │                                                   │     │
│  │ const booking = new Booking({                    │     │
│  │   travelerId: req.user.id,                       │     │
│  │   propertyId, ownerId,                           │     │
│  │   startDate, endDate, totalPrice,                │     │
│  │   status: 'PENDING'                              │     │
│  │ });                                               │     │
│  │ await booking.save();                            │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   │ Booking ID: 507f1f77bcf86cd799439011  │
│                   ↓                                        │
│  Step 4: Publish to Kafka                                 │
│  ┌──────────────────────────────────────────────────┐     │
│  │ Kafka Producer                                    │     │
│  │                                                   │     │
│  │ await producer.send({                            │     │
│  │   topic: 'booking-requests',                     │     │
│  │   messages: [{                                    │     │
│  │     key: booking._id.toString(),                 │     │
│  │     value: JSON.stringify({                      │     │
│  │       bookingId: booking._id,                    │     │
│  │       travelerId, propertyId, ownerId,           │     │
│  │       startDate, endDate, totalPrice,            │     │
│  │       status: 'PENDING',                         │     │
│  │       timestamp: new Date().toISOString()        │     │
│  │     })                                            │     │
│  │   }]                                              │     │
│  │ });                                               │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   │ ✅ Message published                   │
│                   ↓                                        │
│  Step 5: Return response to frontend                      │
│  ┌──────────────────────────────────────────────────┐     │
│  │ res.json({                                        │     │
│  │   message: 'Booking created',                    │     │
│  │   booking                                         │     │
│  │ });                                               │     │
│  └──────────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────────┘
       │
       │ Response: 200 OK
       ↓
┌─────────────┐
│  Traveler   │  Sees confirmation: "Booking created!"
│   (User)    │  Status: PENDING
└─────────────┘

       ↓ ↓ ↓ KAFKA MESSAGE FLOW ↓ ↓ ↓

┌──────────────────────────────────────────────────────────────────────┐
│                    KAFKA BROKER (localhost:9092)                     │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Topic: booking-requests                                             │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ Partition 0                                                  │    │
│  │ ┌─────────────────────────────────────────────────────────┐ │    │
│  │ │ Offset 0: { bookingId: "507...", status: "PENDING", ... }│ │    │
│  │ │ Offset 1: { bookingId: "508...", status: "PENDING", ... }│ │    │
│  │ │ Offset 2: { bookingId: "509...", status: "PENDING", ... }│ │    │
│  │ └─────────────────────────────────────────────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  • Messages are persisted (durable)                                  │
│  • Available for multiple consumers                                  │
│  • Retained based on retention policy (default 7 days)              │
└──────────────────────────────────────────────────────────────────────┘

       ↓ (Optional: Owner Service could consume this for notifications)


PHASE 2: BOOKING ACCEPTANCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────┐
│    Owner    │  6. Owner sees booking request in dashboard
│   (User)    │     and clicks "Accept"
└──────┬──────┘
       │
       │ HTTP PUT /api/bookings/:id/accept
       ↓
┌────────────────────────────────────────────────────────────┐
│             OWNER SERVICE (Producer)                       │
│             http://localhost:3002                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Step 7: Receive accept request                           │
│  ┌──────────────────────────────────────────────────┐     │
│  │ PUT /api/bookings/:id/accept Handler             │     │
│  │                                                   │     │
│  │ • Validate JWT token (must be owner)             │     │
│  │ • Verify booking belongs to this owner           │     │
│  │ • Check status is PENDING                        │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  Step 8: Update database                                  │
│  ┌──────────────────────────────────────────────────┐     │
│  │ MongoDB Update                                    │     │
│  │                                                   │     │
│  │ booking.status = 'ACCEPTED';                     │     │
│  │ booking.updatedAt = new Date();                  │     │
│  │ await booking.save();                            │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   ↓                                        │
│  Step 9: Publish status update to Kafka                   │
│  ┌──────────────────────────────────────────────────┐     │
│  │ Kafka Producer                                    │     │
│  │                                                   │     │
│  │ await producer.send({                            │     │
│  │   topic: 'booking-updates',                      │     │
│  │   messages: [{                                    │     │
│  │     key: booking._id.toString(),                 │     │
│  │     value: JSON.stringify({                      │     │
│  │       bookingId: booking._id,                    │     │
│  │       status: 'ACCEPTED',                        │     │
│  │       updatedBy: 'OWNER',                        │     │
│  │       timestamp: new Date().toISOString()        │     │
│  │     })                                            │     │
│  │   }]                                              │     │
│  │ });                                               │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   │ ✅ Status update published             │
│                   ↓                                        │
│  Step 10: Return response                                 │
│  ┌──────────────────────────────────────────────────┐     │
│  │ res.json({                                        │     │
│  │   message: 'Booking accepted',                   │     │
│  │   booking                                         │     │
│  │ });                                               │     │
│  └──────────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────────┘
       │
       │ Response: 200 OK
       ↓
┌─────────────┐
│    Owner    │  Sees: "Booking accepted!"
│   (User)    │
└─────────────┘

       ↓ ↓ ↓ KAFKA MESSAGE FLOW ↓ ↓ ↓

┌──────────────────────────────────────────────────────────────────────┐
│                    KAFKA BROKER (localhost:9092)                     │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Topic: booking-updates                                              │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ Partition 0                                                  │    │
│  │ ┌─────────────────────────────────────────────────────────┐ │    │
│  │ │ Offset 0: { bookingId: "507...", status: "ACCEPTED", ...}│ │    │
│  │ │ Offset 1: { bookingId: "508...", status: "CANCELLED",...}│ │    │
│  │ └─────────────────────────────────────────────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  • Messages consumed by Booking Service                              │
│  • Enables async status synchronization                             │
└──────────────────────────────────────────────────────────────────────┘
       │
       │ Consumed by consumer group: booking-status-sync-group
       ↓
┌────────────────────────────────────────────────────────────┐
│             BOOKING SERVICE (Consumer)                     │
│             http://localhost:3004                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Step 11: Receive message from Kafka                      │
│  ┌──────────────────────────────────────────────────┐     │
│  │ Kafka Consumer (Always Running)                  │     │
│  │                                                   │     │
│  │ consumer.run({                                    │     │
│  │   eachMessage: async ({ message }) => {          │     │
│  │     const data = JSON.parse(message.value);      │     │
│  │     // data = { bookingId, status, ... }         │     │
│  │   }                                               │     │
│  │ });                                               │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   │ Message received:                      │
│                   │ { bookingId: "507...", status:         │
│                   │   "ACCEPTED", updatedBy: "OWNER" }     │
│                   ↓                                        │
│  Step 12: Synchronize status in database                  │
│  ┌──────────────────────────────────────────────────┐     │
│  │ MongoDB Update                                    │     │
│  │                                                   │     │
│  │ const booking = await Booking.findById(          │     │
│  │   data.bookingId                                 │     │
│  │ );                                                │     │
│  │                                                   │     │
│  │ if (booking) {                                    │     │
│  │   booking.status = data.status;                  │     │
│  │   booking.updatedAt = new Date();                │     │
│  │   await booking.save();                          │     │
│  │   console.log('✅ Booking synced:', data);       │     │
│  │ }                                                 │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   │ ✅ Status synchronized                 │
│                   ↓                                        │
│                [Done - No HTTP response needed]           │
│                 (Event-driven processing)                 │
└────────────────────────────────────────────────────────────┘


PHASE 3: TRAVELER SEES UPDATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────┐
│  Traveler   │  13. Traveler refreshes "My Bookings" page
│   (User)    │
└──────┬──────┘
       │
       │ HTTP GET /api/bookings
       ↓
┌────────────────────────────────────────────────────────────┐
│             TRAVELER SERVICE                               │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Step 14: Query MongoDB                                   │
│  ┌──────────────────────────────────────────────────┐     │
│  │ const bookings = await Booking.find({            │     │
│  │   travelerId: req.user.id                        │     │
│  │ }).sort({ createdAt: -1 });                      │     │
│  └──────────────────────────────────────────────────┘     │
│                   │                                        │
│                   │ Returns: [{ _id: "507...",            │
│                   │           status: "ACCEPTED", ... }]   │
│                   ↓                                        │
│  Step 15: Return to frontend                              │
│  ┌──────────────────────────────────────────────────┐     │
│  │ res.json({ bookings });                          │     │
│  └──────────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────────┘
       │
       │ Response: 200 OK, bookings array
       ↓
┌─────────────┐
│  Traveler   │  Sees: Booking status changed to "ACCEPTED"! 🎉
│   (User)    │  Redux store updated with new status
└─────────────┘


┌──────────────────────────────────────────────────────────────────────┐
│                         KEY BENEFITS                                 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ✅ Decoupling                                                       │
│     • Services don't need to know about each other                  │
│     • Owner Service doesn't call Traveler Service directly          │
│                                                                      │
│  ✅ Scalability                                                      │
│     • Can add more consumers (e.g., Email Service)                  │
│     • Kafka handles load balancing                                  │
│                                                                      │
│  ✅ Reliability                                                      │
│     • Messages persisted even if consumer is down                   │
│     • Automatic retry on failure                                    │
│                                                                      │
│  ✅ Asynchronous Processing                                          │
│     • Owner Service returns immediately                             │
│     • Status sync happens in background                             │
│                                                                      │
│  ✅ Event Sourcing                                                   │
│     • All status changes logged in Kafka                            │
│     • Can replay events for debugging/analytics                     │
└──────────────────────────────────────────────────────────────────────┘
```

## Message Examples

### booking-requests Topic

```json
{
  "bookingId": "507f1f77bcf86cd799439011",
  "travelerId": "507f191e810c19729de860ea",
  "propertyId": "507f1f77bcf86cd799439022",
  "ownerId": "507f191e810c19729de860eb",
  "startDate": "2025-12-01T00:00:00.000Z",
  "endDate": "2025-12-05T00:00:00.000Z",
  "totalPrice": 600,
  "status": "PENDING",
  "timestamp": "2025-11-15T14:30:00.000Z"
}
```

### booking-updates Topic

```json
{
  "bookingId": "507f1f77bcf86cd799439011",
  "status": "ACCEPTED",
  "updatedBy": "OWNER",
  "timestamp": "2025-11-15T15:45:00.000Z"
}
```

## Consumer Groups

- **booking-status-sync-group**: Booking Service subscribes to booking-updates
- Future groups can be added (e.g., email-notification-group)
